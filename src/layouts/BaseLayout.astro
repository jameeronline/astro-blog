---
//types imports
import type { BaseLayoutProps } from "@/types/site";

// styles imports
import "@/styles/global.css";
import "@/styles/starwind.css";

//components imports
import Header from "@components/layouts/Header.astro";
import Footer from "@components/layouts/Footer.astro";
import CookieBanner from "@/components/CookieBanner.astro";
import CTA from "@blocks/CTA.astro";

//fonts import
import "@fontsource-variable/outfit";

//scripts imports
import LocalScript from "@components/scripts/LocalScript.astro";

//site config imports
import { siteConfig } from "@config/site-config.ts";

//page meta data imports
const { title = siteConfig.title, description = siteConfig.description } = Astro.props as BaseLayoutProps;

import { ClientRouter } from "astro:transitions";

const styles = {
  body: "relative h-full font-sans text-gray-900 dark:bg-slate-900 dark:text-slate-50 bg-primary-50/10",
}
---

<html lang="en" class="h-full scroll-animation scroll-smooth">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <link rel="sitemap" href="/sitemap-index.xml" />
    <title>{title}</title>
    <!-- <ClientRouter /> -->
  </head>
  <body
    class={styles.body}
  >
    <!-- <div
      class="absolute inset-0 opacity-50 bg-no-repeat bg-contain bg-left-top -z-10"
      style="background-image: url('/bg-mash-gradient.svg');"
    >
    </div> -->
    <!-- dark overlay -->

    <div class="grid grid-cols-1 grid-rows-[auto_1fr_auto] h-full">
      <Header title={title} />
      <main class="w-full xl:container mx-auto px-4">
        <slot />
      </main>
      <!-- <CTA /> -->
      <Footer />
      <CookieBanner />
    </div>
    <LocalScript />
    <script>
      //   import "../scripts/menu.js";
      //   import "../scripts/vendor.js";
      import { Observer } from "tailwindcss-intersect";
      Observer.start();
    </script>
  </body>
</html>
