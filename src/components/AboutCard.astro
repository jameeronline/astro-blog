---
const { data } = Astro.props;
const { title, description } = data;
---

<div class="flex flex-col gap-6 rounded-lg border border-gray-100 bg-white p-8">
  <h3 class="text-xl font-bold">{title}</h3>
  <p class="text-muted-foreground">
    {description}
  </p>
</div>
