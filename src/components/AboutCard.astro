---
const { data, ...rest } = Astro.props;
const { title, description } = data;
---

<div
  class="flex flex-col rounded-lg border border-gray-100 bg-white p-8 shadow-2xl/10"
  {...rest}
>
  <h3 class="text-xl font-bold">{title}</h3>
  <p class="text-slate-500">
    {description}
  </p>
</div>
